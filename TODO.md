# Future TODO

- ✅ MCP server should use 1Password to store/read its API creds, instead of macOS Keychain (DONE v0.1.8)
  - TODO: Make 1Password account and vault configurable via menu bar settings
  - TODO: Add cache invalidation on app background (10-min TTL currently)
  - TODO: Support multiple 1Password accounts/vaults in future versions
- Open API to LAN should only happen when on a specific WiFi network
